# #Build.io API V1
#
##No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
#The version of the OpenAPI document: v1
#
#Generated by: https://openapi-generator.tech
#Generator version: 7.13.0
#

require "uri"

module Build
  class PipelinesApi
    property api_client : ApiClient

    def initialize(api_client = ApiClient.default)
      @api_client = api_client
    end
    # get a specific pipeline
    # Retrieves details for a specific pipeline
    # @param pipeline_id_or_name [String] Pipeline ID or Name
    # @return [Pipeline]
    def get_pipeline(pipeline_id_or_name : String)
      data, _status_code, _headers = get_pipeline_with_http_info(pipeline_id_or_name)
      data
    end

    # get a specific pipeline
    # Retrieves details for a specific pipeline
    # @param pipeline_id_or_name [String] Pipeline ID or Name
    # @return [Array<(Pipeline, Integer, Hash)>] Pipeline data, response status code and response headers
    def get_pipeline_with_http_info(pipeline_id_or_name : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: PipelinesApi.get_pipeline ..."}
      end
      # verify the required parameter "pipeline_id_or_name" is set
      if @api_client.config.client_side_validation && pipeline_id_or_name.nil?
        raise ArgumentError.new("Missing the required parameter 'pipeline_id_or_name' when calling PipelinesApi.get_pipeline")
      end
      # resource path
      local_var_path = "/api/v1/pipelines/{pipeline_id_or_name}".sub("{" + "pipeline_id_or_name" + "}", URI.encode_path(pipeline_id_or_name.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "Pipeline"

      # auth_names
      auth_names = ["bearer"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"PipelinesApi.get_pipeline",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        cookie_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: PipelinesApi#get_pipeline\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return Pipeline.from_json(data), status_code, headers
    end

    # list apps in a pipeline
    # A list of applications in the pipeline.
    # @param pipeline_id_or_name [String] Pipeline ID or Name
    # @return [Array(App)]
    def list_pipeline_apps(pipeline_id_or_name : String)
      data, _status_code, _headers = list_pipeline_apps_with_http_info(pipeline_id_or_name)
      data
    end

    # list apps in a pipeline
    # A list of applications in the pipeline.
    # @param pipeline_id_or_name [String] Pipeline ID or Name
    # @return [Array<(Array(App), Integer, Hash)>] Array(App) data, response status code and response headers
    def list_pipeline_apps_with_http_info(pipeline_id_or_name : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: PipelinesApi.list_pipeline_apps ..."}
      end
      # verify the required parameter "pipeline_id_or_name" is set
      if @api_client.config.client_side_validation && pipeline_id_or_name.nil?
        raise ArgumentError.new("Missing the required parameter 'pipeline_id_or_name' when calling PipelinesApi.list_pipeline_apps")
      end
      # resource path
      local_var_path = "/api/v1/pipelines/{pipeline_id_or_name}/apps".sub("{" + "pipeline_id_or_name" + "}", URI.encode_path(pipeline_id_or_name.to_s))

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "Array(App)"

      # auth_names
      auth_names = ["bearer"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"PipelinesApi.list_pipeline_apps",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        cookie_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: PipelinesApi#list_pipeline_apps\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return Array(App).from_json(data), status_code, headers
    end

    # list pipelines
    # Lists all pipelines accessible to the current user
    # @return [Array(Pipeline)]
    def list_pipelines()
      data, _status_code, _headers = list_pipelines_with_http_info()
      data
    end

    # list pipelines
    # Lists all pipelines accessible to the current user
    # @return [Array<(Array(Pipeline), Integer, Hash)>] Array(Pipeline) data, response status code and response headers
    def list_pipelines_with_http_info()
      if @api_client.config.debugging
        Log.debug {"Calling API: PipelinesApi.list_pipelines ..."}
      end
      # resource path
      local_var_path = "/api/v1/pipelines"

      # cookie parameters
      cookie_params = Hash(String, String).new

      # query parameters
      query_params = Hash(String, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, (String | ::File)).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "Array(Pipeline)"

      # auth_names
      auth_names = ["bearer"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"PipelinesApi.list_pipelines",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        cookie_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: PipelinesApi#list_pipelines\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return Array(Pipeline).from_json(data), status_code, headers
    end
  end
end
